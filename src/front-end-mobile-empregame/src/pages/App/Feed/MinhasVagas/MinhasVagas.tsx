import { ScrollView, Text } from "native-base";

export const MinhasVagas = () => {
  return (
    <ScrollView flex={1} paddingX={"30px"} paddingTop={"20px"}>
      <Text>MinhasVagas</Text>
    </ScrollView>
  );
};
