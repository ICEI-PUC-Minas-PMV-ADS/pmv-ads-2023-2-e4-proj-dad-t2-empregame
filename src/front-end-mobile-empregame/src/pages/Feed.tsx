import { View } from "native-base";
import { Text } from "react-native-svg";

export const Feed = () => {
  return (
    <View flex={1} flexDirection={"column"}>
      <Text>Logado Feed</Text>
    </View>
  );
};
